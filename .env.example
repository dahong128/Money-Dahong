# Copy to `.env` and fill in values.

# --- Binance (Spot) ---
BINANCE_API_KEY=
BINANCE_API_SECRET=

# --- Bot ---
# 说明：
# - `run`（EMA）优先读取 `configs/ema_cross.toml`
# - `run-ma` / `backtest` / `backtest-grid` 优先读取 `configs/ma_cross.toml`
# - 下面 SYMBOL/INTERVAL 作为配置文件留空时的回退值
SYMBOL=ETHUSDT
INTERVAL=1m
TRADING_MODE=dry_run  # dry_run|live
CONFIRM_LIVE_TRADING=  # set to YES to allow live trading

# --- Risk (MVP defaults; tune later) ---
# 每次 BUY 的“名义金额（USDT）”硬上限（安全帽）。
# - 即使 `configs/ma_cross.toml` 配置了更大的固定名义/复利投入，也会被这个值截断。
MAX_ORDER_NOTIONAL_USDT=25
COOLDOWN_SECONDS=60

# --- Telegram Alerts ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Logging ---
LOG_LEVEL=INFO
