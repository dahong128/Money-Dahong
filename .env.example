# Copy to `.env` and fill in values.

# --- Binance (Spot) ---
BINANCE_API_KEY=
BINANCE_API_SECRET=

# --- Bot ---
SYMBOL=ETHUSDT
INTERVAL=1m
TRADING_MODE=dry_run  # dry_run|live
CONFIRM_LIVE_TRADING=  # set to YES to allow live trading

# --- Strategy (EMA Cross) ---
EMA_FAST_PERIOD=12
EMA_SLOW_PERIOD=26

# --- Strategy (MA Cross / 双均线) ---
# `money-dahong run-ma` 会优先读取 `configs/ma_cross.toml`。
# 下面这些环境变量仅作为兜底（当你在 toml 里不填 symbol/interval 等时才会用到）。
MA_TYPE=sma  # sma|ema
MA_FAST_PERIOD=20
MA_SLOW_PERIOD=60

# --- Risk (MVP defaults; tune later) ---
# 每次 BUY 的“名义金额（USDT）”硬上限（安全帽）。
# - 即使 `configs/ma_cross.toml` 配置了更大的固定名义/复利投入，也会被这个值截断。
MAX_ORDER_NOTIONAL_USDT=25
COOLDOWN_SECONDS=60

# --- Telegram Alerts ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Logging ---
LOG_LEVEL=INFO
