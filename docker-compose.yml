services:
  bot:
    build:
      context: .
    env_file:
      - .env
    restart: unless-stopped
    command: ["run-ma"]
    volumes:
      - ./configs:/app/configs:ro

  cli:
    build:
      context: .
    env_file:
      - .env
    entrypoint: ["money-dahong"]
    profiles: ["cli"]
    volumes:
      - ./configs:/app/configs:ro
